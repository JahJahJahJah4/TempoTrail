import React from "react";
import ReactDOM from "react-dom/client";
import TempoScapeMobile from "./TempoScapeMobile";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TempoScapeMobile />
  </React.StrictMode>
);
